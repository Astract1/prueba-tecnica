//Este archivo contiene la definición de la consulta GetUserQuery, que se utiliza para obtener un usuario en el sistema.
export class GetUserQuery {
    constructor(public readonly id: string) {}
  }